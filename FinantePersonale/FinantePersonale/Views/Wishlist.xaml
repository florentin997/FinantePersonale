<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:FinantePersonale.ViewModels"
             x:Class="FinantePersonale.Views.Wishlist">

    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:WishlistVM x:Key="vm"/>
            <vm:IstoricListaObiecteVM x:Key="vmListItems"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Salveaza"
                     Command="{Binding Source={StaticResource vm}, Path=SaveWishlistItemCommand}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>

        <StackLayout>

            <StackLayout Margin="75,0,75,0" BindingContext="{Binding Source={StaticResource vm}}">
                <Entry x:Name="WishlistItem"
                       Placeholder="Item"
                       HorizontalTextAlignment="Center"
                       Text="{Binding ItemW, Mode=TwoWay}"/>
                <Entry x:Name="ItemValue"
                       Placeholder="Costul"
                       HorizontalTextAlignment="Center"
                       Text="{Binding IValue, Mode=TwoWay}"/>
            </StackLayout>

            <ListView x:Name="itemListView"
                      ItemsSource="{Binding Source={StaticResource vmListItems}, Path=IstWishlistItems}">
                    <!--ItemSelected="ListView_ItemSelected">-->
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding ItemName}"
                                  Detail="{Binding Id}"/>

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>


            <Button x:Name="deleteButtonWishlist"
                    HorizontalOptions="Center"
                    VerticalOptions="End"
                    Text="STERGERE"
                    BackgroundColor="red"
                    Clicked="deleteButtonWishlist_Clicked">
                
            </Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

<!--
<TextCell Text="{Binding Item}"/>
 datele ajung in Item, dar nu sunt afisate 
 CRED CA AICI E PROBLEMA, de verificat la WishlistVM->WishlistItemsVM
    -->
<!--
    <ContentPage.Content>
        <StackLayout>
            <Label Text="AICI VA FI LISTA CU DORINTE!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>-->