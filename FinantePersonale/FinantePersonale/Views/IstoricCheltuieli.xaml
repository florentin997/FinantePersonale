<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:FinantePersonale.ViewModels"  
             x:Class="FinantePersonale.Views.IstoricCheltuieli">
    <!--xmlns, ns=namespace -->
    <!-- in pag asta afisez valorile -->
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:IstoricCheltuieliVM x:Key="vm"/> 
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ListView ItemsSource="{Binding Source={StaticResource vm}, Path=IstCheltuieli}" IsPullToRefreshEnabled="True"> 
            <!--StaticResource ptr ca am definit resursa in ResourceDictionary, avand cheia "vm"-->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding SumaCh}" 
                              TextColor="Black" 
                              Detail="{Binding SubcategorieCh}">
                        <TextCell.ContextActions>
                            <MenuItem Text="Delete"
                                      CommandParameter="{Binding}"
                                      IsDestructive="True"
                                      Clicked="MenuItem_Clicked">
                            </MenuItem>
                        </TextCell.ContextActions>
                    </TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>